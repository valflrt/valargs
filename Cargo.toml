[package]
name = "valargs"
description = "A simple command-line argument helper for parsing positional arguments and options."
version = "0.1.2"
authors = ["valflrt"]
license = "MIT"
edition = "2024"

[dependencies]
